<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/appMain</item>
        <item name="colorPrimaryDark">@color/appMain</item>
        <item name="android:statusBarColor">#1e7566</item>
        <item name="colorAccent">@color/appMain</item>

        <item name="android:textColorHint">@color/appMain</item>
    </style>

    <style name="FooBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#ffffff</item>
        <item name="colorPrimaryDark">#ffffff</item>
        <item name="colorAccent">#ffffff</item>


    </style>










</resources>
